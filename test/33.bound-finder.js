'use strict';
const BoundFinder = require('../src/shared/bound-finder');

describe('BoundFinder', function () {
	it('returns true when an upper bound is found');
	it('returns true when a lower bound is found');
	it('can inherit the state of a previous instance');
});
