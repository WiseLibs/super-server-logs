'use strict';
const UniqueQueue = require('../src/shared/unique-queue');

describe('UniqueQueue', function () {
	it('allows pushing and shifting values in FIFO order');
	it('allows deleting any value in the queue');
	it('throws if a dupicate item is added to the queue');
});
